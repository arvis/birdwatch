# Milestones

## Baseline (pre-GSD)

**Shipped before GSD tracking began.**

Features delivered:
- Bird identification endpoint (`POST /api/identify`) with OpenAI GPT-4o vision
- Example images from Wikimedia Commons (provider pattern)
- Wikipedia links for identified species
- Mobile home screen — camera capture + gallery picker
- Mobile result screen — species info, confidence badge, example gallery
- Backend unit + integration test suite
- Android emulator local dev setup (10.0.2.2 routing)

Last phase: 4 (pre-GSD numbering from docs/specs)

---
*GSD tracking starts at v1.0*
